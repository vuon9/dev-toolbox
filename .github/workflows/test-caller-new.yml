name: test-caller-new

on:
  workflow_dispatch:
  pull_request:

permissions:
  id-token: write
  contents: read
  pull-requests: write
  issues: read

jobs:
  call:
    uses: ./.github/workflows/test-reusable-call2.yml
    with:
      mode: pr
    secrets:
      OPENCODE_API_KEY: ${{ secrets.OPENCODE_API_KEY }}
