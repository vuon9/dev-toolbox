name: test-reusable

on:
  workflow_dispatch:
  pull_request:

jobs:
  test-call:
    uses: ./.github/workflows/opencode-review-reusable.yml
    secrets:
      OPENCODE_API_KEY: ${{ secrets.OPENCODE_API_KEY }}
