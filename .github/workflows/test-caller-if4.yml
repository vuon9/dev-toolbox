name: test-caller-if4

on:
  workflow_dispatch:
  pull_request:

jobs:
  call:
    uses: ./.github/workflows/test-reusable-call.yml
    with:
      mode: pr
    secrets:
      OPENCODE_API_KEY: ${{ secrets.OPENCODE_API_KEY }}
